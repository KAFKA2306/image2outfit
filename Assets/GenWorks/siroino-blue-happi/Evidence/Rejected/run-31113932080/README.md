# Rejected render checkpoint: run 31113932080

This directory preserves every image and report produced by the final hosted Blender validation run before the checkpoint merge. Its multiview and pose-review files are byte-identical to directly inspected run 31113251116, so the same detached sleeves, floating collar, and pose failures apply. It is rejected evidence, not an approved preview.
