# Rejected render checkpoint: run 31092626753

This directory preserves the complete five-view and six-pose render output. Direct visual review failed because the body is excessively box-shaped, the sleeves separate during poses, and the collar/shoulder construction floats. The images are evidence of a failed iteration, not approved product previews.

Render evidence is committed before review disposition; failed iterations remain traceable in Git history instead of existing only as transient workflow artifacts.
