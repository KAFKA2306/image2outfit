# Pre-Blender stage contracts v1

## 対象

正規13工程のうち、Blenderを起動する前の6工程を型付き成果物として固定します。

1. `ingest-reference` → `ReferenceSet`
2. `normalize-view` → `NormalizedReferenceSet`
3. `decompose-garment` → `GarmentDecomposition`
4. `draft-patterns` → `PatternHypothesisSet`
5. `infer-stitches` → `SeamHypothesisSet`
6. `initialize-3d` → `ArrangementPlan`

## 参照画像

原本、原本から作った派生画像、生成画像を区別します。原本は取得元、取得時刻、利用条件、リポジトリ内ファイル、SHA-256を必須とします。URLだけでは工程を通過できません。背面構造、摩擦、裏地など観測不能な情報は`unknown_fields`へ残します。

## 視点正規化

カメラ内部・外部パラメータ、変換行列と逆行列、身体ランドマーク、遮蔽mask、鏡像状態、不確実性を保存します。逆変換による再投影誤差と、焦点距離・画素・姿勢の不確実性から型紙寸法へ伝播する誤差を計算します。

## 衣装分解

画像領域だけでなく、衣装部品の種類、身体位置、構造役割、レイヤー、可視・遮蔽・推定状態を保存します。不可視部は複数仮説として保持します。衣装部品と型紙パネルを一対一へ固定しません。正準語彙にない種類は名前空間付き拡張として隔離します。

## 型紙仮説

既存の`ConstructionSpec`を正本とし、身体寸法と部位別ゆとりから導いた寸法の来歴、原画像への再投影証拠、不可視項目を仮説ごとに保持します。自己交差、極小面積、極小辺、異常角をBlender前に拒否できます。同じ正本からJSON、SVG、ASCII DXF previewを決定論的に再生成します。

## 縫合仮説

縫合は型紙辺IDの組として保存します。縫い方向、開始点対応、縫合形式、ゆとり分布、ギャザー比、意図された開口を独立して表現します。未縫合、過剰縫合、辺長、向き、未知辺、必要副資材を原因別に監査します。

## 三次元初期配置

パネルごとに身体ランドマーク、位置、回転、身体offset、巻き方向、明示的レイヤー順、表裏、境界箱を保存します。`StylingSpec`のアンカー、折り、タック、開閉、レイヤー、非対称操作を順序付き拘束へ展開します。未解決交差、同層重複、裏返りをsolver開始前に拒否し、可逆操作は通常着装へ戻せます。

## 完了境界

この契約は #146〜#151 の前段工程を対象とします。Blender編集可能形状、実クロスシミュレーション、FBX、レンダリング証拠、完成判定は #152以降が所有します。前段契約の成功だけで製品を`COMPLETE`にはしません。
