[project]
name = "image2outfit"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
  "Pillow==12.3.0",
]

[dependency-groups]
dev = [
  "ruff",
]
snapshot = [
  "matplotlib",
  "numpy",
  "scipy",
  "trimesh",
]
